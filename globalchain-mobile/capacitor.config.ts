import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.globalchain.mobile',
  appName: 'Global-Chain Mobile',
  webDir: 'dist'
};

export default config;
